<Page x:Class="ConstructionCompanyApp.CustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Заказчики">

    <Grid Margin="15" Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" Background="#4CAF50" Padding="10" CornerRadius="5">
            <TextBlock Text="🏠 СПИСОК ЗАКАЗЧИКОВ" FontSize="18" FontWeight="Bold" 
                       Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <!-- Панель поиска -->
        <Border Grid.Row="1" Background="#E8F5E9" Padding="10" Margin="0,10,0,10" CornerRadius="5">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="🔍 Поиск:" VerticalAlignment="Center" FontWeight="Bold" Margin="0,0,10,0"/>
                <TextBox x:Name="tbSearch" Width="250" Height="30" 
                         TextChanged="tbSearch_TextChanged" Background="White"/>
            </StackPanel>
        </Border>

        <!-- Таблица -->
        <DataGrid x:Name="dgCustomers" Grid.Row="2" AutoGenerateColumns="True" 
                  IsReadOnly="True" AlternatingRowBackground="#E8F5E9" 
                  FontSize="13" BorderBrush="#4CAF50" BorderThickness="1"/>
    </Grid>
</Page>